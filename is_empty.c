#include "Header.h"

/**
* Prueft, ob der Stapel leer ist.
*
* @param stack_1 ist der Stapel.
* @param up ist das oberste Element des Stapels.
* @return -1, wenn der Stapel leer ist, sonst 0;
*/

int is_empty(struct stack *stack_1, char *up)
{
	if (*up == -1)
	{
		return -1;
	}
	else
	return 0;
}
